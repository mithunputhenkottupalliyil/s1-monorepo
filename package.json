{
    "private": true,
    "devDependencies": {
        "lerna": "^3.20.2"
    },
    "name": "react-app",
    "scripts": {
        "bootstrap-x": "lerna bootstrap",
        "test-x":"lerna run test --scope={components,utils}",
        "dev-build" : "lerna run boom --scope=hub"
    }
}
