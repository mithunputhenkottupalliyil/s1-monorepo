{
    "private": true,
    "devDependencies": {
        "lerna": "^3.21.0"
    },
    "name": "react-app",
    "scripts": {
        "bootstrap-x": "lerna bootstrap",
        "test-x": "lerna run test --scope={components,utils, hub}",
        "dev-build": "lerna run boom --scope=hub"
    },
    "workspaces": [
        "packages/*"
    ]
}
